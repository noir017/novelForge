# 发布区

这里放**发布章节**，是这本书的唯一真相：摘要从这里生成，全书上下文从这里取正文。

章是从 `.novelforge/manuscripts/` 里那份生成正文切出来的——作者在编辑器里用单独一行
`---` 标出断点，点「拆成章节」，正文按标记切成若干章落到这里，中转站里那份随即删掉。
一章正文可能切成两章，也可能一次只切一章（不标 `---` 就是这样）。

老工程直接把已有的章放进来就行：没有细纲不影响，`chapters/` 里有文件就算写完了那一章。

创作流水线见 [../README.md](../README.md)。
